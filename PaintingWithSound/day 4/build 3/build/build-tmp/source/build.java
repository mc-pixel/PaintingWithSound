/* autogenerated by Processing revision 1283 on 2022-07-14 */
import processing.core.*;
import processing.data.*;
import processing.event.*;
import processing.opengl.*;

import java.util.HashMap;
import java.util.ArrayList;
import java.io.File;
import java.io.BufferedReader;
import java.io.PrintWriter;
import java.io.InputStream;
import java.io.OutputStream;
import java.io.IOException;

public class build extends PApplet {

int    stageW     = 1200;
int    stageH     = 900;
int  clrBG      = 0xFF242424;
String pathAssets = "../../../assets/";

String whichImg = pathAssets + "colors_002.png";
PImage clrs; // image
int    clrsW; // width of image
float  clrCount;
float  clrSpeed = 0.02f; // the speed of the color change
float  clrOffset = 0.01f; // the distance from each of the squares getting colored
//**************************************************************************************************************************************************************/
PImage tex;

 public void settings() {
	size(stageW, stageH, P3D);
}

 public void setup() {
	background(clrBG);
	clrs = loadImage(whichImg);
	clrsW = clrs.width-1;
	tex = loadImage(pathAssets + "tex1.png");
}

 public void draw() {
	background(clrBG);
	textureMode(NORMAL);
	strokeWeight(0);
	noStroke();

	float wave = sin( clrCount );
	float waveMap = map(wave, -1, 1, 0, clrsW);
	tint( clrs.get((int)waveMap, 0) );
	push();
		translate(stageW/2,stageH/2,0);
		scale(800);
		beginShape(QUADS);//quads expects 4 points only

			texture(tex);
			vertex(-(0.5f), -(0.5f), 0,	 0, 0); //// x, y, z, u, v
			vertex((0.5f), -(0.5f), 0, 	1, 0); //// x, y, z, u, v
			vertex((0.5f), (0.5f), 0, 	1, 1); //// x, y, z, u, v
			vertex(-(0.5f), (0.5f), 0,	 0, 1); //// x, y, z, u, v
		endShape(CLOSE);//this closes the shape
	pop();
	//rect(0, 0, 100, 100);

	clrCount += clrSpeed;

}


  static public void main(String[] passedArgs) {
    String[] appletArgs = new String[] { "build" };
    if (passedArgs != null) {
      PApplet.main(concat(appletArgs, passedArgs));
    } else {
      PApplet.main(appletArgs);
    }
  }
}
